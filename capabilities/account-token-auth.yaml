naftiko: 1.0.0-alpha2
info:
  label: Envestnet Account Token APIs — Auth
  description: 'Envestnet Account Token APIs — Auth. 2 operations. Lead operation: Envestnet Generate Access Token. Self-contained
    Naftiko capability covering one Envestnet business surface.'
  tags:
  - Envestnet
  - Auth
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENVESTNET_API_KEY: ENVESTNET_API_KEY
capability:
  consumes:
  - type: http
    namespace: account-token-auth
    baseUri: ''
    description: Envestnet Account Token APIs — Auth business capability. Self-contained, no shared references.
    resources:
    - name: auth-token
      path: /auth/token
      operations:
      - name: generateaccesstoken
        method: POST
        description: Envestnet Generate Access Token
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: deletetoken
        method: DELETE
        description: Envestnet Delete Token
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
  exposes:
  - type: rest
    namespace: account-token-auth-rest
    port: 8080
    description: REST adapter for Envestnet Account Token APIs — Auth. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/auth/token
      name: auth-token
      description: REST surface for auth-token.
      operations:
      - method: POST
        name: generateaccesstoken
        description: Envestnet Generate Access Token
        call: account-token-auth.generateaccesstoken
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletetoken
        description: Envestnet Delete Token
        call: account-token-auth.deletetoken
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: account-token-auth-mcp
    port: 9090
    transport: http
    description: MCP adapter for Envestnet Account Token APIs — Auth. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: envestnet-generate-access-token
      description: Envestnet Generate Access Token
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: account-token-auth.generateaccesstoken
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: envestnet-delete-token
      description: Envestnet Delete Token
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: account-token-auth.deletetoken
      outputParameters:
      - type: object
        mapping: $.
