naftiko: 1.0.0-alpha2
info:
  label: Envestnet Aggregation APIs — Configs
  description: 'Envestnet Aggregation APIs — Configs. 4 operations. Lead operation: Envestnet Get Subscribed Notification
    Events. Self-contained Naftiko capability covering one Envestnet business surface.'
  tags:
  - Envestnet
  - Configs
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ENVESTNET_API_KEY: ENVESTNET_API_KEY
capability:
  consumes:
  - type: http
    namespace: account-aggregation-configs
    baseUri: ''
    description: Envestnet Aggregation APIs — Configs business capability. Self-contained, no shared references.
    resources:
    - name: configs-notifications-events
      path: /configs/notifications/events
      operations:
      - name: getsubscribednotificationevents
        method: GET
        description: Envestnet Get Subscribed Notification Events
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: eventName
          in: query
          type: string
          description: Name of the webhook subscription event
    - name: configs-notifications-events-eventName
      path: /configs/notifications/events/{eventName}
      operations:
      - name: updatesubscribednotificationevent
        method: PUT
        description: Envestnet Update Notification Subscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: eventName
          in: path
          type: string
          description: Name of the webhook subscription event
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: createsubscriptionnotificationevent
        method: POST
        description: Envestnet Subscribe For Notification Event
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: eventName
          in: path
          type: string
          description: Name of the webhook subscription event
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
      - name: deletesubscribednotificationevent
        method: DELETE
        description: Envestnet Delete Notification Subscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: eventName
          in: path
          type: string
          description: Name of the webhook subscription event
          required: true
  exposes:
  - type: rest
    namespace: account-aggregation-configs-rest
    port: 8080
    description: REST adapter for Envestnet Aggregation APIs — Configs. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/configs/notifications/events
      name: configs-notifications-events
      description: REST surface for configs-notifications-events.
      operations:
      - method: GET
        name: getsubscribednotificationevents
        description: Envestnet Get Subscribed Notification Events
        call: account-aggregation-configs.getsubscribednotificationevents
        with:
          eventName: rest.eventName
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/configs/notifications/events/{eventname}
      name: configs-notifications-events-eventname
      description: REST surface for configs-notifications-events-eventName.
      operations:
      - method: PUT
        name: updatesubscribednotificationevent
        description: Envestnet Update Notification Subscription
        call: account-aggregation-configs.updatesubscribednotificationevent
        with:
          eventName: rest.eventName
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createsubscriptionnotificationevent
        description: Envestnet Subscribe For Notification Event
        call: account-aggregation-configs.createsubscriptionnotificationevent
        with:
          eventName: rest.eventName
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deletesubscribednotificationevent
        description: Envestnet Delete Notification Subscription
        call: account-aggregation-configs.deletesubscribednotificationevent
        with:
          eventName: rest.eventName
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: account-aggregation-configs-mcp
    port: 9090
    transport: http
    description: MCP adapter for Envestnet Aggregation APIs — Configs. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: envestnet-get-subscribed-notification-events
      description: Envestnet Get Subscribed Notification Events
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: account-aggregation-configs.getsubscribednotificationevents
      with:
        eventName: tools.eventName
      outputParameters:
      - type: object
        mapping: $.
    - name: envestnet-update-notification-subscription
      description: Envestnet Update Notification Subscription
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: account-aggregation-configs.updatesubscribednotificationevent
      with:
        eventName: tools.eventName
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: envestnet-subscribe-notification-event
      description: Envestnet Subscribe For Notification Event
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: account-aggregation-configs.createsubscriptionnotificationevent
      with:
        eventName: tools.eventName
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: envestnet-delete-notification-subscription
      description: Envestnet Delete Notification Subscription
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: account-aggregation-configs.deletesubscribednotificationevent
      with:
        eventName: tools.eventName
      outputParameters:
      - type: object
        mapping: $.
